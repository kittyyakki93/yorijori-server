# 로컬 개발: IntelliJ / 터미널에서 --spring.profiles.active=local 로 실행
# MySQL에 DB whatthefood 생성 후, application-auth.properties 에 계정 설정
spring.application.front.domain=localhost
spring.application.front.host=http://localhost:3000
spring.datasource.url=jdbc:mysql://localhost:3306/whatthefood
